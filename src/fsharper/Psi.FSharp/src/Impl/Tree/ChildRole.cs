namespace JetBrains.ReSharper.Psi.FSharp.Impl.Tree
{
  public static class ChildRole
  {
    // general common roles
    public const short NONE = 0;

    // Common roles should live between NONE and LAST

    // general common roles
    public const short LAST = 100;
  }
}